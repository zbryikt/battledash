extends /auth/base.pug
block content
  img.mb-2.mx-a(src="/assets/img/c/logo/ldio-alt.svg",style="width:100px")
  h3.mb-0 Reset Your Password
  p.text-muted.mb-4.text-sm please be sure to pick a password that is strong enough.
  form#password-reset.form.text-left.m-a(method="POST", style="width:400px")
    .from-group.mb-3
      label Type the new password
      input.form-control(name="password",type="password",placeholder="at least 8 chars")
      .invalid-feedback password too weak
    .from-group.mb-3
      label Type it again
      input.form-control(name="confirm",type="password",placeholder="")
      .invalid-feedback password not matched
    input.btn.btn-success.btn-block.mt-2.disabled(type="submit",value="Set Pasword")
    input(type="hidden",name="_csrf")
    +nbr(2)
    .text-muted: small
      p Good password should be long enough ( at least 10 chars ). Prevent using your name, phone number or your birthday as password.
      p You can check how #[a(href="https://en.wikipedia.org/wiki/Password_strength",target="_blank") Password Strength] is measured to know how safe your password is.
  .ldcv.ldcvmgr(data-name="reset-password-invalid"): .base: .inner.card.m-auto.shadow-sm(style="width:600px")
    .card-body: .p-4
      .text-center
        h1 Invalid Code
        p Your password reset link doesn't work.
      br
      .text-muted
        p The password reset link you received is either expired or somehow invalid. But don't worry, you can #[a(href="/auth/reset/") head directly to the password reset form] and request another new password reset link.
        p If this keeps on happening, you can also #[a(href="#",onclick="lda.ldcvmgr.toggle('contact')") contact us], we will help you reset the password for your account.

      br
      a.btn.btn-primary.btn-lg.btn-block(href="/auth/reset/") Resent Reset Link
  +script("/js/auth/reset/change.js")
